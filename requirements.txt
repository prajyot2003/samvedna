# core/ has no dependencies at all and must stay that way (DECISIONS.md D3).
# Everything here belongs to the service layer.
sqlalchemy>=2.0
pytest>=8.0
faster-whisper>=1.0
numpy>=1.24
soundfile>=0.12
opensmile>=2.5
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
httpx>=0.27
psycopg[binary]>=3.1
